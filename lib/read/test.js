var child_process = require('child_process');var path = require('path');var cmd = process.argv[0] + ' ' + path.join(__dirname, 'readline');var options = {	stdio: [ process.stdin ]};console.log('process.stdin.isRaw: ' + process.stdin.isRaw);process.stdout.write('Input a line: ');var buf = child_process.execSync(cmd, options);console.log('What inputed is: ' + buf);// process.stdin.on('data', function (buf) {// 	if (buf.toString() == '\u0003') console.log('on ctrl-c') && process.exit();// });// process.on('SIGINT', function () {// 	console.log('SIGINT');// })